# react-seedling-ts
A starter react 16 project with webpack and typescript

* get nodejs and git installed on your machine


npm install -g typescript 
npm install -g webpack
npm install -g yarn (optional)

git clone https://github.com/swaggerloaf/react-seedling-ts.git

cd react-seedling-ts

npm install (or yarn install)
npm start (or yarn start)

